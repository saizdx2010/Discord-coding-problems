// this function returns a Something before every sentences that is passed in
const giveMeSomething = (sentence) => {
    return `Something ${sentence}`;
};

console.log(giveMeSomething("is better than nothing"))
