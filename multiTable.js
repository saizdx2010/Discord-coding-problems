function multiTable(number) {
    // good luck
        hasil = '';
        for (let i = 1; i <=10; i++){
            hasil = console.log(`${i} * ${number} = ${i * number}`);

        }
        return `${hasil}\n`;
}

  multiTable(10)